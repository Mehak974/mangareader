"use client";

// Adcash removed — their aclib.js script injects onclick pop-unders
// site-wide regardless of zone type. Keeping this stub so existing
// <AdCashBanner /> imports don't break.
export default function AdCashBanner() {
  return null;
}
